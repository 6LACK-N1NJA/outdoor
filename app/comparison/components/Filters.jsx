'use client'
import { useState } from 'react'

import ExpandFiltersButton from './ExpandFiltersButton'
import KnowMore from './KnowMore'

export default function Filters({ filters, selectedFilters, title }) {
  const [isExpanded, setIsExpanded] = useState(false)
  return (
    <section
      className={`transition delay-30 duration-700 ease-in-out rounded-bottom-md border-2 sticky bg-opacity-10 border-t-white ${
        isExpanded ? ' bg-stone-200 shadow-lg' : ' bg-stone-100 shadow-sm'
      }`}
    >
        <div className='flex'>
        <h1 className=' text-2xl'>{title}</h1>
        <KnowMore>Here will be text generated by gpt for this specific category. Seo optimized. May be pretty long</KnowMore>
        </div>
      {isExpanded && filters.map((f) => <p className='ml-7' key={`${f}_filter`}>{f}</p>)}
      <button className='text-sm text-zinc-400 w-1/2 mx-10 text-center ' onClick={() => setIsExpanded(!isExpanded)}>{isExpanded ? 'Colapse Filters 👆' : 'Expand Filters 👇'}</button>
    </section>
  )
}
